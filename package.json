{
  "name": "vercel-test",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "fastify start -l info api/app.js",
    "dev": "fastify start -w -l info -P api/app.js"
  },
  "dependencies": {
    "fastify": "^4.25.2"
  },
  "devDependencies": {
    "fastify-cli": "^6.1.1"
  }
}
